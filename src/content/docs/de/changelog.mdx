---
title: Änderungsprotokoll
---

import Windows from "~/components/platform/Windows.astro"
import MacOS from "~/components/platform/MacOS.astro"

Versionen von RIM werden durch Überschriften auf Ebene 3 gekennzeichnet. Wesentliche Updates mit mehreren Funktionen für beide Plattformen werden in Unterüberschriften für Mac und Windows auf Ebene 4 unterteilt.

### 3.4.20

* Dies ist ein Update zur Behebung von Fehlern.

### 3.4.19

* macOS: Die Audioausgabe von macOS-Zielen weist nun eine durchweg geringere Latenz auf.
* macOS: RIM ist nun mit der kürzlich veröffentlichten Version 16.1.0 von Karabiner Elements kompatibel.

### 3.4.18

* Dies ist ein Update zur Fehlerbehebung.

### 3.4.17

* Dies ist ein Update zur Fehlerbehebung.

### 3.4.16

* Dies ist ein Update zur Fehlerbehebung.

### 3.4.15

* Wenn Sie die Fernzugriffsfunktion starten, während auf dem Controller ein anderer Screenreader als NVDA ausgeführt wird, funktioniert der Wechsel des Screenreaders auf dem Controller nun auch mit NVDA 2026.1.

### 3.4.14

* Dies ist ein Update zur Fehlerbehebung.

### 3.4.13

* Die Fernzugriffsfunktion von RIM ist nun mit NVDA 2026.1 kompatibel.

### 3.4.12

* Dies ist ein Update zur Fehlerbehebung.

### 3.4.11
Diese Version implementiert eine Text-Chat-Funktion. Chat-Nachrichten werden gesprochen und visuell angezeigt, sodass sie per Sprache, Braille und visuell vorgelesen werden können. Die Transkripte werden nach der Sitzung lokal gespeichert.  
Beachten Sie, dass die Chat-Funktion für Unternehmen streng optional ist. Aktivieren Sie die Funktion über den Abschnitt „Einschränkungen” im Dashboard, wenn Sie die Nutzung der Funktion in Ihrem gesamten Unternehmen zulassen möchten.

### 3.4.10

* Dieses Update enthält interne Änderungen zur Verbesserung der Zuverlässigkeit.

### 3.4.9

* Windows: Es wurde ein Problem behoben, das bei einigen Computern beim Aktualisieren auf die vorherige Version auftrat.

### 3.4.8

* Implementiert die Erfassung des Remote-Zugriffsmoduls während einer Sitzungsaufzeichnung über das System-Audio des steuernden Computers.
* Implementiert einen Dialog zur Einholung der Zustimmung auf der Zielseite vor Beginn einer Sitzungsaufzeichnung während aller keywordbasierten Sitzungen. Sollte der Zielbenutzer die Aufzeichnungsanfrage ablehnen, wird die Sitzung beendet.
* Implementiert einen Bestätigungsdialog vor dem manuellen Beenden einer Aufzeichnung. Dieser Dialog kann auf Wunsch so konfiguriert werden, dass er nicht angezeigt wird.
* Es ist nicht mehr möglich, eine Aufzeichnung zu starten, während eine Sitzung umgeschaltet wird.
* Implementiert die Möglichkeit, das Standardverzeichnis für Aufzeichnungen sowie mehrere andere Konfigurationsparameter über eine TOML-Datei festzulegen. Anweisungen zum Konfigurieren dieser Optionen finden Sie im Abschnitt über die Sitzungsaufzeichnung.

### 3.4.7
Diese Version führt eine offene Beta-Version der Sitzungsaufzeichnungsfunktion für beide Plattformen ein. Diese Funktion kann von jedem kostenlos genutzt werden, und der Cloud-Dienst wird niemals für die Verarbeitung der Aufzeichnung verwendet. Die Aufzeichnungen werden derzeit im lokalen Videordner des steuernden Benutzers gespeichert, d. h. unter Windows im Ordner „Videos” und unter MacOS im Ordner „Movies”.  
Bitte zögern Sie nicht, uns Feedback zu senden (https://getrim.app/feedback), wenn Sie bei der Verwendung dieser Funktion auf Probleme stoßen oder wenn Sie Kommentare oder Vorschläge zur Verbesserung haben.

### 3.4.6

* Dies ist ein Bugfix-Update.

### 3.4.5

* Dies ist ein Update zur Fehlerbehebung.

### 3.4.4

#### macOS
RIM ist jetzt mit den aktuellen Versionen des Tastaturbelegungstools Karabiner Elements kompatibel.

### 3.4.3

#### Windows
Die Remote-Zugriffsfunktion ist jetzt mit NVDA 2025.1 kompatibel.

### 3.4.2

#### macOS
Beim Umschalten einer Sitzung beendet RIM jetzt den Remote-Zugriff, wenn diese Funktion auf dem vorherigen Ziel aktiv war.

### 3.4.1

#### Windows
Beim Ausführen eines vorkonfigurierten Zielinstallationsprogramms im unbeaufsichtigten Modus registriert RIM nun den Zielcomputer zuverlässiger und akzeptiert unbeaufsichtigte Verbindungen.

### 3.4.0
* Für Unternehmensbenutzer wurde eine Option zum Erstellen von Installationsprogrammen ohne Ablaufdatum hinzugefügt.
* Mit dieser Änderung geht eine Validierungsprüfung für alle benutzerdefinierten Installationsprogramme einher. Wenn ein benutzerdefiniertes Installationsprogramm abgelaufen ist oder gelöscht wurde, wird es nicht mehr ausgeführt.
* Es wurde ein Problem behoben, das zu einem konstanten Piepton von Zielcomputern ohne integrierte Audiofunktion führte, insbesondere bei Windows-Servern.
* Es ist nicht mehr möglich, versehentlich Windows+Umschalt+Rücktaste auf einem Windows-Ziel mit macOS als Controller zu drücken.


### 3.3.39

#### macOS
Dem Begrüßungsbildschirm wurde ein Hinweis zu den erforderlichen Berechtigungen hinzugefügt.

### 3.3.38

* Es wurde ein weiteres Problem behoben, das zu einer ungenauen Zuordnung zwischen Controller- und Ziel-Mauszeiger führte. Bitte teilen Sie uns mit, wenn Sie weiterhin Probleme in diesem Bereich haben.

### 3.3.37

* Es wurde ein Problem behoben, das zu einer ungenauen Zuordnung zwischen Controller- und Ziel-Mauszeiger führte.

### 3.3.36

* Die Zuverlässigkeit der RIM-Unterstützung für Verbindungen über strenge Firewalls wurde verbessert.

### 3.3.35

* Dies ist ein Update zur Fehlerbehebung.

### 3.3.34

#### Windows
Es wurde ein Problem behoben, das dazu führte, dass der Mauszeiger im Remote-Sitzungsfenster des Controllers nicht sichtbar war, wenn eine Verbindung zu einigen Windows-Zielen ohne angeschlossene Maus hergestellt wurde.

### 3.3.33

#### macOS
RIM verfügt nun über die erforderliche Berechtigung von Apple, um den Fernzugriff, einschließlich des unbeaufsichtigten Zugriffs, zu ermöglichen, ohne jeden Monat erneut die Erlaubnis zur Bildschirmaufzeichnung anzufordern. Dazu müssen Zielbenutzer, die macOS Sequoia ausführen, nur einmal eine neue „Fernzugriffs”-Berechtigung erteilen. RIM fordert diese Berechtigung an, bevor der Bildschirm „Hilfe erhalten” angezeigt wird oder beim ersten Start nach der Installation dieses Updates auf Computern, die als unbeaufsichtigte Ziele registriert sind. Beachten Sie, dass es noch ein bekanntes Problem mit RIM unter macOS Sequoia gibt: Die Fernzugriffsfunktion auf macOS Sequoia-Zielcomputern funktioniert nicht richtig. Wir arbeiten derzeit mit Apple zusammen, um dieses Problem zu beheben. Wenn Sie oder Ihre Zielbenutzer von diesem Problem betroffen sind, empfehlen wir Ihnen weiterhin, bis auf Weiteres kein Upgrade auf macOS Sequoia durchzuführen. Vielen Dank für Ihre Geduld.

### 3.3.32

* Es wurde ein Problem behoben, das manchmal zum Absturz von RIM führte, wenn ein Ordner in die Zwischenablage kopiert wurde.
* Es wurde ein Problem behoben, das manchmal zum Einfrieren der Sitzung und zur erneuten Verbindung führte, wenn ein großer Ordner in die Zwischenablage kopiert wurde.

#### Windows
Es wurde ein Problem behoben, das dazu führte, dass RIM zeitweise nicht erkannte, wenn eine neue Datei oder eine Reihe von Dateien in die Zwischenablage kopiert wurde.

### 3.3.31

#### macOS
Die Anweisungen zum Erteilen der erforderlichen Berechtigungen unter macOS Sequoia wurden aktualisiert. Beachten Sie, dass es unter macOS Sequoia noch zwei bekannte Probleme mit RIM gibt: macOS fordert den Zielbenutzer jeden Monat erneut zur Erteilung der Bildschirmaufzeichnungsberechtigung auf, was unbeaufsichtigte Sitzungen beeinträchtigt, und die Remote-Zugriffsfunktion auf macOS Sequoia-Zielcomputern funktioniert nicht ordnungsgemäß. Wir arbeiten derzeit mit Apple zusammen, um diese Probleme zu beheben. Wenn Sie oder Ihre Zielbenutzer von einem dieser Probleme betroffen sind, empfehlen wir Ihnen weiterhin, bis auf Weiteres kein Upgrade auf macOS Sequoia durchzuführen. Vielen Dank für Ihre Geduld.

### 3.3.30

* Es wurde ein Problem behoben, das die zuverlässige Funktion der Funktion „Protokolle senden” beeinträchtigte.

### 3.3.29

* Dieses Update enthält interne Änderungen am RIM-Software-Updater, um dessen Stabilität zu verbessern.

### 3.3.28

Dieses Update ermöglicht die Installation eines zusätzlichen Serverprofils. Dies ist nützlich für Empfänger von technischem Support, die möglicherweise Support von einer Organisation erhalten, die eine private RIM-Bereitstellung anstelle unserer öffentlichen Cloud verwendet, sowie für Controller, die möglicherweise mit mehr als einem Server gleichzeitig arbeiten.

### 3.3.27

* Dieses Update enthält einige Änderungen an der Art und Weise, wie RIM den Mauszeiger rendert und Mauseingaben verarbeitet. Es behebt einige Situationen, in denen RIM zwei Mauszeiger im Inhaltsbereich des Fensters „Remote-Sitzung” anzeigte, sowie einen bekannten Fall, in dem in diesem Bereich kein Zeiger angezeigt wurde, insbesondere wenn keine Maus an den Zielcomputer angeschlossen war. Bei der Verbindung mit Windows-Zielen kann RIM nun häufig die Mauszeigereinstellungen des Controllers innerhalb des Fensters „Remote-Sitzung” verwenden, wodurch es für Benutzer mit Sehbehinderungen besser zugänglich ist. Außerdem ist die Umwandlung der Mauseingaben vom Display des Controllers zum Zieldisplay nun genauer.

### 3.3.24

* Das Fenster „Remote Session“ kann nun innerhalb eines einzelnen Displays oder über mehrere Displays hinweg verschoben und in der Größe angepasst werden. Die Titelleiste dieses Fensters verfügt nun auch über die plattformüblichen Steuerelemente zum Schließen, Maximieren usw. des Fensters. Beachten Sie, dass sich das RIM-Symbol, auf das Sie klicken können, um das Menü zu öffnen, nun in der Mitte der Titelleiste und nicht mehr am linken Ende befindet.

#### Windows
Es wurde ein Fehler behoben, der dazu führte, dass RIM beim Kopieren von Text in die Zwischenablage in Adobe Reader abstürzte.

### 3.3.23

* Der Controller kann nun die Audioausgabe des Ziels stummschalten. Aktivieren Sie dazu beim Starten einer schlüsselwortbasierten Sitzung das Kontrollkästchen „Audioausgabe des Ziels stummschalten“ oder wählen Sie „Audioausgabe des Ziels stummschalten“ aus dem Sitzungsmenü.

### 3.3.22

* Im Fenster „Hilfe bereitstellen” zeigt RIM nun, während es darauf wartet, dass der Zielbenutzer das Schlüsselwort eingibt, das von Ihnen eingegebene Schlüsselwort sowie einen Countdown bis zum Ablauf des Schlüsselworts an. Es gibt auch eine Schaltfläche „Abbrechen”, mit der Sie den aktuellen Versuch einfach abbrechen können, ohne das Fenster vollständig zu schließen. Dies ist besonders praktisch, wenn Sie ein anderes Schlüsselwort ausprobieren möchten.
* Abonnenten von RIM Enterprise und RIM Pro mit dem Enterprise-Add-on können nun bestimmte RIM-Funktionen innerhalb der Organisation einschränken. Derzeit können die folgenden Funktionen eingeschränkt werden: Sitzungswechsel, Sprachkonversationen, Controller, die unbeaufsichtigten Zugriff anfordern, Übertragung von Text und Dateien zwischen Computern über die Zwischenablage und Controller, die den Zielcomputer sperren. Organisationsadministratoren können über den Eintrag „Einschränkungen“ im Dropdown-Menü „Organisation“ im RIM-Dashboard auf diese Einstellungen zugreifen.

### 3.3.21

* Ein weiteres Problem wurde behoben, das dazu führte, dass die RIM-App zeitweise die Verbindung verlor.

### 3.3.20

* Es wurde ein Problem behoben, das manchmal dazu führte, dass ein Computer, auf dem RIM ausgeführt wurde, die Verbindung zum RIM-Dienst trennt und nicht automatisch wieder eine Verbindung herstellt, sodass RIM neu gestartet werden musste.

### 3.3.19

* Bei der Übertragung von Dateien über die Option im RIM-Menü zeigt RIM nun den Prozentsatz der Fertigstellung in der Titelleiste des Fortschrittsfensters an.

#### Windows
RIM verwendet nun eine robustere Methode für den Übergang vom Windows-Anmeldebildschirm zum Hauptdesktop. Dies sollte insbesondere auf Ziel-PCs hilfreich sein, auf denen der Benutzer kein Windows-Passwort hat.

#### macOS
Bei Neuinstallationen fragt RIM nun nach dem Begrüßungsbildschirm nach Berechtigungen, wobei die angeforderten Berechtigungen davon abhängen, ob Sie Hilfe erhalten oder bereitstellen möchten.

### 3.3.18

#### Windows
Es wurde ein Problem behoben, das dazu führte, dass RIM sich aufhängte, wenn die Sitzung während einer Dateiübertragung beendet oder erneut verbunden wurde.


### 3.3.17


#### Windows
Es wurde ein Problem behoben, das dazu führte, dass der RIM Windows-Installer von Microsoft SmartScreen als ungewöhnlich heruntergeladen und potenziell unsicher markiert wurde.


### 3.3.14

* Es wurde ein Problem in der vorherigen Aktualisierung behoben, das die Lokalisierung für einige Sprachen verhinderte.

### 3.3.13

* Die japanische Lokalisierung wurde dank der Beiträge von Kojiro Kuyo aktualisiert.
* Verschiedene Fehlerbehebungen.

### 3.3.12

* Japanische Lokalisierung hinzugefügt. Wir freuen uns über Ihr Feedback zur Qualität und Vollständigkeit der Übersetzung.
* Verbesserungen der französischen Übersetzung – vielen Dank an Nael Sayegh.

### 3.3.11

Behebung eines Regressionsfehlers, der dazu führte, dass RIM in einigen Netzwerken unnötigerweise die Relay-Server nutzte, was zu einer Verschlechterung der Verbindungsqualität führte.

### 3.3.10

Behebung von sporadischen JavaScript-Fehlern, die in der vorherigen Version gemeldet wurden.


### 3.3.9

* Einführung einer neuen Fallback-Methode für die Verbindung zu unseren weltweiten Relays, die auch in eingeschränkten Netzwerken funktioniert, die nur ausgehende HTTPS-Verbindungen zulassen. Dadurch entfällt die Notwendigkeit, Firewall-Ausnahmen für RIM hinzuzufügen, außer in extrem eingeschränkten Umgebungen.
* RIM sucht beim Starten der Anwendung nicht mehr im Vordergrund nach Updates. Es sucht nun nur noch im Hintergrund nach Updates. Wenn Sie RIM zu einer sofortigen Aktualisierung zwingen möchten, können Sie die neue Schaltfläche „Nach Updates suchen“ im Fenster „Über“ verwenden.
* Wenn bei einer umgekehrten Sitzungsumschaltung der ursprüngliche Controller einen Bildschirmleser ausführt, das Ziel jedoch nicht, startet RIM nun automatisch die Remote-Zugänglichkeit auf dem Ziel, wie es beim Starten der Sitzung der Fall war.


#### Windows

Es wurde ein Problem behoben, das manchmal die Wiedergabe von NVDA-Soundeffekten bei Verwendung der Remote-Zugänglichkeit verhinderte.

### 3.3.8

#### Windows

Dieses Update führt die Unterstützung für die neue Version NVDA 2024.1 in der Remote-Zugriffsfunktion von RIM ein.

### 3.3.7

* Weitere kleinere Lokalisierungsfehlerbehebungen.

### 3.3.6

#### macOS
Die vorherige Version für diese Plattform enthielt die Lokalisierungsfehlerbehebungen nicht.

### 3.3.5

* Dieses Update enthält Korrekturen für die schwedische Lokalisierung, dank Karl-Otto Rosenqvist.
* In allen Sprachen wurde nun der Menüpunkt „Beenden” für das RIM-Symbol in der Taskleiste übersetzt.

### 3.3.4

* Niederländische und brasilianisch-portugiesische Übersetzungen hinzugefügt.

### 3.3.3

* Wir sind nun so überzeugt von der Lokalisierung von RIM, dass wir sie aus der Beta-Phase nehmen können. RIM verwendet nun standardmäßig die in Ihrem System eingestellte Sprache, und wir haben die Bezeichnung „BETA” aus der Sprachauswahl entfernt.
* Um Verwirrung zu vermeiden, gibt es auf dem Bildschirm „Hilfe bereitstellen” keine Schaltfläche „Abbrechen” mehr. Stattdessen können Sie das Fenster einfach schließen.
* Wenn Sie auf dem Bildschirm „Hilfe erhalten” auswählen, diesen Computer zu Ihrem RIM-Konto hinzuzufügen, bleibt das Fenster „Hilfe erhalten” nun geöffnet.

### 3.3.2
#### Windows
* Es wurde ein Problem behoben, das RIM daran hinderte, Updates auf einigen Computern mit einer gesperrten Konfiguration zu installieren.

### 3.3.1
* Aktualisierte Übersetzungen
* Interne Änderungen


### 3.3.0
Dieses Update enthält eine Reihe neuer Funktionen sowie plattformspezifische Überarbeitungen. Zu den allgemeinen Ergänzungen gehören:
* Unterstützung für Sprachkonversationen in unbeaufsichtigten Sitzungen hinzugefügt. Der Zielbenutzer wird vor Ort um Erlaubnis gebeten, eine Sprachkonversation zu starten.
* Bei Sprachkonversationen verarbeitet RIM nun das Anschließen und Trennen von Mikrofonen während der Sitzung. 

#### Windows
* Der Narrator spricht nun konsistent RIM-Benachrichtigungen.
* Hinzufügen einer Nicht-Admin-Fallback-Lösung.

#### Mac OS
* Hinzufügen eines über den Info-Bildschirm zugänglichen Deinstallationsprogramms, das RIM vollständig und sauber entfernt, einschließlich des Karabiner Virtual HID-Treibers.
* Die Informationen zum Mac-Zielcomputer umfassen nun auch laufende Prozesse und installierte Anwendungen. 

### 3.2.18
Wenn Sie im Fenster „Hilfe bereitstellen“ auf die Schaltfläche „Abmelden“ klicken, kehrt RIM nun zum Fenster „Hilfe erhalten“ zurück, anstatt Sie zur erneuten Anmeldung aufzufordern.
### 3.2.17
* Probleme mit den Übersetzungen von Menüelementen wurden behoben.
* Beim Starten einer unbeaufsichtigten Sitzung unterstützt RIM nun das Doppelklicken auf eine Zielgruppe oder einen Zielcomputer.
* Ein Fehler beim Hinzufügen eines Zielcomputers zu Ihrem Konto bei Verwendung von Single Sign-On (SSO) wurde behoben.

### 3.2.16
* Probleme mit der französischen Lokalisierung wurden behoben. Vielen Dank an Nael Sayegh für diese Korrekturen. Dies ist der erste Beitrag der Community zu unseren Lokalisierungsbemühungen.

### 3.2.15
#### Windows
* Es wurde ein Problem behoben, das dazu führte, dass die Bildschirmausgabe auf dem Controller beim Sperren des Zielcomputers einfror.

### 3.2.14
#### Windows
* RIM erkennt Fusion, das Vispero-Produkt, das JAWS und ZoomText kombiniert, nun als eigenständiges Produkt, wenn es ermittelt, welcher Bildschirmleser auf dem Zielcomputer ausgeführt wird, anstatt es einfach als JAWS zu erkennen. Das bedeutet, dass RIM nicht versucht, seine Remote-Zugriffsfunktion zu starten, während Fusion hochfährt.
* Wir haben Berichte erhalten, dass Fusion nach dem Start einer RIM-Sitzung einige Tastenanschläge vom Controller ignorierte, wie es früher bei JAWS der Fall war. Wir gehen davon aus, dass dieses Problem nun behoben ist.

### 3.2.13
#### Windows
Es wurde ein Problem mit der Tastatureingabe beim Herstellen einer Verbindung zu Zielcomputern behoben, auf denen JAWS 2024 ausgeführt wird.

### 3.2.12
Ein selten auftretendes Problem, das die Verbindung der RIM-App mit dem Cloud-Dienst verhindern konnte, wurde umgangen.

### 3.2.11
Dem Fenster „Verbindungsdetails“ wurde eine neue Schaltfläche hinzugefügt, mit der Sie den Zielcomputer auffordern können, seine RIM-Protokolle an Pneuma Solutions zu senden. Bitte verwenden Sie diese Funktion nur, wenn Sie von Pneuma Solutions dazu aufgefordert werden.

### 3.2.10
Es wurde ein Problem behoben, das dazu führen konnte, dass RIM bis zum nächsten Neustart der Anwendung nicht mehr funktionierte, wenn die Internetverbindung des Computers vorübergehend nicht verfügbar war.
### 3.2.9
#### Mac OS
Leider können wir auf dieser Plattform keine automatische Stereomikrofoneingabe bei Sprachgesprächen mehr anbieten, da dies in einigen Fällen dazu führte, dass die Mikrofoneingabe auf einem Mac überhaupt nicht mehr funktionierte. Beachten Sie, dass diese Änderung keine Auswirkungen auf die Audioausgabe des Zielgeräts hat, die weiterhin in Stereo erfolgt.

### 3.2.8
#### Mac OS
Es wurde ein Fehler behoben, der verhinderte, dass RIM gestartet wurde, wenn die Anwendung über den Finder oder das Dock statt über die globale Tastenkombination aktiviert wurde.
### 3.2.7
Sprachkonversationen verwenden nun automatisch Stereo, wenn die Audio-Hardware dies unterstützt. Dies ist nützlich, wenn RIM für Schulungen in der Audioproduktion verwendet wird.
### 3.2.6
Ein kürzlich aufgetretenes Problem wurde behoben, das dazu führen konnte, dass RIM auf einer neuen Betriebssysteminstallation nicht funktionierte.
### 3.2.5
Die Meldung, die die verbleibende freie Zeit anzeigt, wurde korrigiert.
### 3.2.4
Die Lokalisierung von RIM in andere Sprachen befindet sich derzeit in der Beta-Phase. Derzeit werden die folgenden Sprachen unterstützt: Deutsch, Spanisch, Französisch, Italienisch und Schwedisch. Um RIM in einer dieser Sprachen zu verwenden, klicken Sie unten im Fenster „Hilfe anbieten“ oder „Hilfe erhalten“ auf die Schaltfläche „Sprache (BETA)“, wählen Sie dann Ihre Sprache aus der Dropdown-Liste aus und klicken Sie auf die Schaltfläche „Sprache wechseln“. Wir freuen uns auf Ihr Feedback.
### 3.2.1
* Der Start einer neuen RIM-Sitzung auf der Controllerseite verläuft nun reibungsloser. Das ursprüngliche Fenster, in dem Sie Ihr Schlüsselwort eingegeben oder einen Zielcomputer ausgewählt haben, bleibt so lange sichtbar, bis das Hauptfenster der Sitzung bereit ist. Wenn das ursprüngliche Fenster zu Beginn der Sitzung nicht mehr im Fokus ist, z. B. weil Sie während des Wartens auf die Eingabe eines Schlüsselworts durch den Zielbenutzer zu einer anderen Aufgabe gewechselt sind, benachrichtigt Sie RIM, dass die Sitzung bereit ist, und das Hauptfenster der Sitzung wird angezeigt, sobald Sie den Fokus wieder auf RIM setzen.
* Nur macOS: Im Rahmen der vorherigen Änderung haben wir die Art und Weise geändert, wie RIM sich selbst im Dock und in der Befehlstaste+Tab-Liste hinzufügt und entfernt. Bitte teilen Sie uns mit, wenn Sie Probleme mit dem aktuellen Verhalten feststellen.
* Nur Windows-Ziele: Wenn ein Bildschirmleseprogramm auf dem Zielcomputer gestartet wird, während die Fernzugriffsfunktion aktiv ist, wird die Fernzugriffsfunktion stillschweigend beendet, um eine doppelte Sprachausgabe zu vermeiden.

### 3.2.0
Erste offizielle Version für Mac OS! Zu den neuen Funktionen gehören:
* Möglichkeit, unbeaufsichtigte oder aufgeforderte Sitzungen innerhalb einer interaktiven Sitzung zu konfigurieren.
* Völlig neue Berechtigungsanweisungen für Benutzer, die VoiceOver nicht verwenden.

### 3.1.49
Diese Version bietet eine konsistente, plattformübergreifende Möglichkeit, das RIM-Controller-Sitzungsmenü mit der Maus zu öffnen. Ein Controller kann entweder auf das große RIM-Symbol in der oberen linken Ecke des Sitzungsfensters klicken oder, wie bisher unter Windows möglich, mit der rechten Maustaste auf eine beliebige Stelle in der Titelleiste klicken.
### 3.1.48
#### Mac OS
* Unterstützung für die FN+Zifferntasten-Umgehungslösung zur Verwendung der Funktionstastenreihe auf MacBooks hinzugefügt.
* RIM für Mac passt nun die Menüleiste an, insbesondere die Option „Über“ im Anwendungsmenü öffnet nun unseren eigenen „Über“-Dialog anstelle des generischen Dialogs.

### 3.1.47
#### Windows
Beim Verbinden eines Mac-Controllers mit einem Windows-Ziel, wobei beide Seiten dasselbe nicht-amerikanische Tastaturlayout verwendeten, wurden die Tastatureingaben vom Controller nicht korrekt übersetzt. Version 3.1.47 für Windows behebt dieses Problem.
### 3.1.46
Diese Version bietet Unterstützung für das neue RIM Personal Community Support-Abonnementpaket.

### 3.1.45
#### Mac OS
RIM kann nun die Erfassung von Bildschirminhalten und Audioausgabe automatisch fortsetzen, wenn der Zielcomputer während einer Sitzung zum Sperrbildschirm wechselt.

### 3.1.44
#### Mac OS
* Implementiert Neustart und erneute Verbindung für Mac-Ziele, einschließlich Standard- und Notfall-Neustarts.
* Wie beim unbeaufsichtigten Zugriff wird RIM aufgrund von Plattformbeschränkungen, insbesondere FileVault, nicht auf dem macOS-Anmeldebildschirm angezeigt. Der Zielbenutzer muss sich daher erneut anmelden, bevor RIM eine erneute Verbindung herstellen kann.
* Die in der vorherigen Aktualisierung eingeführte Benachrichtigung über Dateiübertragungen wird nun nur noch einmal pro RIM-Sitzung angezeigt.

### 3.1.43
#### Mac OS
Dieses Update ändert die Benutzeroberfläche für Dateiübertragungen. Wenn eine oder mehrere Dateien auf einer der beiden Seiten der Verbindung in die Zwischenablage kopiert werden, erhält der Controller nun eine Benachrichtigung, dass die Dateiübertragung über das RIM-Menü abgeschlossen werden kann. Wenn der Controller diese Menüoption auswählt, wird die Dateiübertragung durchgeführt, und nach Abschluss wird auf dem Zielcomputer ein temporärer Ordner mit der übertragenen Datei oder den übertragenen Dateien geöffnet. Diese Methode der Dateiübertragung ist erforderlich, wenn der Zielcomputer ein Mac ist. Wenn das Ziel ein Windows-Computer ist, kann die Dateiübertragung entweder mit dieser neuen Methode oder mit der alten Methode des Einfügens im Datei-Explorer auf dem Zielcomputer abgeschlossen werden.
### 3.1.42
#### Mac OS
* RIM für Mac funktioniert jetzt als unbeaufsichtigtes Ziel. Es führt auch Updates im Hintergrund durch, wie es die Windows-Version tut.
* Wir müssen noch einige Arbeiten am Zugriff auf den Sperrbildschirm vornehmen und glauben aufgrund der Einschränkungen der Plattform nicht, dass wir den Zugriff auf den Anmeldebildschirm vor der Anmeldung eines Benutzers ermöglichen können.
* Wir haben noch nicht herausgefunden, wie oder ob wir vorkonfigurierte Zielinstallationsprogramme implementieren können. Daher müssen Sie eine der anderen verfügbaren Methoden zum Einrichten des unbeaufsichtigten Zugriffs verwenden.
* Beseitigt eine Ursache für Latenz bei Audioaufnahmen von Mac-Zielen.

### 3.1.41
#### Windows
Es wurde ein Problem behoben, das manchmal dazu führte, dass RIM als Reaktion auf Aktivitäten in der Windows-Zwischenablage während einer RIM-Sitzung abstürzte.
### 3.1.40
Es wurden einige weitere Protokollierungen in Bezug auf Audio auf dem Ziel hinzugefügt. Wenn Sie also verzögertes Audio auf dem Ziel reproduzieren können, tun Sie dies bitte mit dieser Version und senden Sie dann die Protokolle.
### 3.1.39
#### Mac OS
Dieses Update fügt Mausunterstützung für Mac-Ziele hinzu.
RIM fordert nun die Berechtigung zur Verwendung der macOS-Barrierefunktionen an. Bei Neuinstallationen ersetzt dies die Berechtigungsanfrage für die Eingabeüberwachung, sodass
die Gesamtzahl der Berechtigungsschritte nicht erhöht wird. Bei Updates müssen Sie jedoch
diese Berechtigung erteilen, bevor Sie RIM nach dem Update verwenden können.
### 3.1.38
#### Mac OS
Ein Absturz, der Mac-Ziele bei der Sitzungsinitialisierung betraf, wurde behoben.
### 3.1.37
Dieses Update markiert einen wichtigen Meilenstein in der Mac-Portierung von RIM. Die Remote-
Barriereigkeitsfunktion von RIM ist jetzt verfügbar, wenn eine Verbindung zu Mac-Zielen hergestellt wird.
Durch eine spezielle Konfiguration von VoiceOver auf dem Zielcomputer
hört der Controller, unabhängig davon, ob er Windows oder Mac verwendet, Sprache,
, während der Zielbenutzer keine Sprach- oder Soundeffekte hört. Es gibt auch keinen sichtbaren Hinweis darauf, dass VoiceOver auf dem Zielgerät läuft.
Der Startvorgang für den Fernzugriff, entweder automatisch oder manuell, funktioniert genauso wie bei Windows-Zielen. Und natürlich wird die spezielle Konfiguration von VoiceOver am Ende der Sitzung beendet und VoiceOver wird auf seine vorherigen Einstellungen zurückgesetzt.
Beachten Sie, dass sowohl Windows- als auch Mac-Controller nun die Version 3.1.37 ausführen müssen, um die volle Funktionalität bei der Verbindung mit einem Mac-Ziel zu erhalten.
### 3.1.36
#### Windows
Es wurde ein Absturz behoben, der beim Starten der Sitzung auf Windows-Zielen auftrat.

Beachten Sie, dass sowohl Windows- als auch Mac-Controller nun die Version 3.1.37 ausführen müssen, um die volle Funktionalität bei der Verbindung mit einem Mac-Ziel zu erhalten.
### 3.1.36
#### Windows
Die Remote-Zugriffsfunktion ist nun mit NVDA 2023.1 kompatibel.
### 3.1.35
#### Mac OS
* RIM verwendet nun durchgängig Stereo-Audio, insbesondere für Mac-Controller.
* Bei Verwendung von RIM für Mac als Controller mit der Remote-Zugriffsfunktion verwendet RIM nun die Stimme und Sprechgeschwindigkeit, die im Abschnitt „Gesprochene Inhalte” der Systemeinstellungen definiert sind.

### 3.1.34
#### Mac OS
Wenn ein Mac-Controller mit einem Windows-Ziel verbunden ist, auf dem kein Screenreader ausgeführt wird, können Sie nun das Remote-Zugriffsmodul auf dem Windows-Ziel ausführen und die Sprachausgabe auf dem Mac-Controller erhalten. Beachten Sie, dass Sie für die Interaktion mit dem Windows-Ziel weiterhin NVDA-Tastaturbefehle und nicht VoiceOver-Befehle verwenden müssen und dass wir das NVDA-Laptop-Tastaturlayout verwenden. 
### 3.1.33
Aufgrund von Nutzer-Feedback haben wir den Soundeffekt bei der Anzeige von Benachrichtigungen zum Status einer Remote-Sitzung entfernt. 
### 3.1.32
#### Mac OS
RIM unterstützt jetzt die Fn-Taste, wenn ein Mac-Laptop als Controller verwendet wird. 
Beachten Sie, dass RIM die obere Reihe der Funktionstasten immer als F1 bis F12 behandelt und Sie müssen die Fn-Taste verwenden, wenn Sie diese Tasten als Systemfunktionstasten (zum Einstellen der Lautstärke usw.) verwenden möchten, unabhängig davon, wie die Systemeinstellungen auf beiden Seiten der Verbindung festgelegt sind. Wir gehen davon aus, dass Sie diese Tasten in einer Remote-Sitzung ohnehin meistens als F-Tasten verwenden möchten. Funktionen wie die Verwendung von Fn+Pfeil nach links zum Drücken der Home-Taste funktionieren wie erwartet.  
### 3.1.31
#### Mac OS
* RIM hat zuvor die Sperre für Tastatureingaben nicht aufgehoben, wenn bestimmte Dialogfelder angezeigt wurden, z. B. das Dialogfeld, in dem um die Erlaubnis zum Zugriff auf das Mikrofon gebeten wurde. Dies wurde nun behoben.
* Bei einer Verbindung von Mac zu Windows oder umgekehrt kann der Controller das RIM-Menü auf dem Zielcomputer nicht mehr aufrufen. Diese Korrektur erfordert das entsprechende Update für RIM für Windows.

#### Windows
* Es wurde eine Möglichkeit hinzugefügt, die Protokolldateien von RIM an Pneuma Solutions zu senden, die in Version 3.1.29 für Mac OS eingeführt wurde. Bitte verwenden Sie diese Funktion nur, wenn Sie von einem Vertreter von Pneuma Solutions dazu aufgefordert werden.
* RIM gibt nun bei jeder Anzeige einer Benachrichtigung einen Ton aus.

### 3.1.30
#### Mac OS
RIM fordert nun sowohl beim ersten Start als auch bei bestehenden Benutzern, die ein Update von einer früheren Version durchführen, die Berechtigung zur Anzeige von Benachrichtigungen an. Wie bei den meisten anderen Berechtigungen muss auch diese Berechtigung in den Systemeinstellungen aktiviert werden. Wir haben außerdem einen eigenen Ton für diese Benachrichtigungen hinzugefügt.
### 3.1.29
#### Mac OS
* Wir glauben, dass das von einigen Benutzern gemeldete Problem mit der Anforderung der Berechtigung zur Überwachung der Tastatureingabe auf Rechnern, auf denen Karabiner Elements läuft, nun behoben ist.
* Der RIM-Dialog „Über“ verfügt nun über eine Schaltfläche zum Senden von Protokollen. Bitte verwenden Sie diese nur, wenn Sie von Pneuma Solutions dazu aufgefordert werden.

### 3.1.28
#### Mac OS
Bei Verwendung von RIM als Controller ist es nun mit erweiterten Tools zur Tastaturanpassung wie Karabiner Elements kompatibel.
### 3.1.27
#### Mac OS
Dies ist ein kleines Update mit nur zwei Änderungen:
* RIM wurde als macOS 13.0 (Ventura) oder höher erforderlich markiert.
* RIM wurde so korrigiert, dass die virtuelle Tastatur nur verbunden ist, wenn RIM tatsächlich als Ziel läuft. Das bedeutet, dass der Tastatur-Einrichtungsassistent nicht angezeigt wird, wenn RIM als Controller verwendet wird. Wir suchen noch nach Möglichkeiten, diesen Dialog ganz zu vermeiden.

### 3.1.26
Erste Beta-Version von RIM für Mac OS.
### 3.1.24
* Bei der Verbindung mit einem Zielcomputer mit einer niedrigeren Bildschirmauflösung oder einem höheren DPI-Skalierungsfaktor als der Controller skaliert RIM nun die Anzeige des Remote-Ziels so, dass sie auf den Bildschirm des Controllers passt, wodurch die Remote-Anzeige visuell besser zu bearbeiten ist.

### 3.1.23
* Unterstützung für Prepaid-Stunden hinzugefügt.

### 3.1.22
* Fehler behoben, der manchmal dazu führte, dass RIM auf dem Zielcomputer in eine Schleife geriet, die unbeaufsichtigte Verbindungen verhinderte.

### 3.1.21
* Es wurde eine neue Tastenkombination hinzugefügt: Windows+Umschalt+M. Diese minimiert das Fenster in einem Sitzungsfenster und gibt den Tastaturfokus an den lokalen (Controller-)Rechner zurück.

### 3.1.20
* Es wurde eine Protokollierung hinzugefügt, die uns bei der Diagnose von Berichten über unbeaufsichtigte Ziele hilft, die zeitweise von RIM getrennt werden.

### 3.1.19
* Einige Sonderfälle bei der Verarbeitung von Remote-Tastatureingaben wurden behoben. Insbesondere wenn JAWS auf dem Zielcomputer ausgeführt wird, erkennt JAWS nun korrekt das Drücken der Pause- und NumLock-Tasten vom Controller. Bitte teilen Sie uns mit, wenn Sie Regressionen bei der Verarbeitung von Tastatureingaben feststellen, insbesondere bei Nicht-US-Tastaturlayouts.

### 3.1.18
* Es wurde ein Problem behoben, das verhinderte, dass RIM sich auf Zielgeräten aktualisierte, die sich auf dem Windows-Anmeldebildschirm befanden und keine aktive Benutzersitzung hatten.

### 3.1.17
* Seit dem letzten Update haben wir Berichte erhalten, dass die Umschalttaste auf dem Zielcomputer beim Ausführen von JAWS hängen bleibt. Wir gehen davon aus, dass dieses Update das Problem behebt. Bitte teilen Sie uns mit, wenn Sie weiterhin Probleme mit hängenden Modifikatortasten haben.
* Es wurde ein Fehler behoben, der dazu führte, dass RIM NVDA am Ende einer Sitzung auf dem Zielcomputer herunterfuhr, wenn NVDA auf dem Zielcomputer nach der Aktivierung der Remote-Zugriffsfunktion gestartet wurde.

### 3.1.16
* Wenn JAWS auf dem Zielcomputer mit aktivierter einheitlicher Tastaturverarbeitung ausgeführt wird, reagiert JAWS nun zuverlässig auf Tastenanschläge vom Controller.
* Es wurde ein Fehler behoben, der sporadisch beim Starten des Add-ons für den Fernzugriff nach der Installation oder Aktualisierung des NVDA-Add-ons auf dem Controller-Computer auftrat.

### 3.1.15
* Wenn Sie eine Verbindung zu einem Zielcomputer herstellen, auf dem kein Bildschirmleseprogramm ausgeführt wird, aktiviert RIM automatisch die Remote-Zugriffsfunktion, wenn Sie ein Bildschirmleseprogramm auf dem Controller-Computer ausführen. Wenn Sie auf dem Controller-Computer ein anderes Bildschirmleseprogramm als NVDA ausführen, beendet RIM dieses Bildschirmleseprogramm sogar automatisch und startet NVDA, sofern Sie es installiert haben.
* Es wurden Probleme behoben, die das Remote-Zugriffsmodul daran hinderten, Ihre lokalen NVDA-Konfigurationseinstellungen wie Tastaturlayout und Tastaturecho zu verwenden.

### 3.1.14
* Der Prozess zum Wiederherstellen einer Verbindung bei Verbindungsproblemen ist jetzt robuster.
* Es wurde ein Problem behoben, das manchmal dazu führte, dass der Hintergrundaktualisierungsprozess von RIM angehalten wurde.
* Wenn auf dem Controller eine ältere Version von RIM als auf dem Zielgerät ausgeführt wird, enthält das Menü keine Option mehr zum Aktualisieren des Zielgeräts.
* Wenn ein Screenreader oder das Remote-Zugriffsmodul von RIM auf dem Zielcomputer ausgeführt wird, können Sie es jetzt über eine Option im Menü beenden.

### 3.1.13
* Es wurde ein Fehler behoben, der dazu führte, dass Benutzer nach der Installation einiger Windows-Updates aus RIM abgemeldet wurden.

### 3.1.12
* Auf vielfachen Wunsch fordert RIM am Ende jeder Sitzung nicht mehr zur Abgabe eines Kommentars auf. Sie können weiterhin Kommentare zu Sitzungen im RIM-Dashboard veröffentlichen. Möglicherweise fügen wir in Zukunft eine konfigurierbare Option hinzu, um zur Abgabe eines Kommentars aufzufordern.

### 3.1.11
* Wenn auf dem Zielcomputer mindestens diese Version von RIM ausgeführt wird, aber noch kein Update auf die neueste Version durchgeführt wurde, kann der Controller nun über eine neue Option im Sitzungsmenü ein Update der RIM-Installation auf dem Zielcomputer erzwingen.

### 3.1.10
* Sie können nun optional am Ende einer RIM-Sitzung einen Kommentar hinzufügen. Sitzungskommentare können im RIM-Dashboard angezeigt werden.
* Das Fenster „Verbindungsdetails“ wurde verbessert. Die Statistiken werden live aktualisiert, solange das Fenster geöffnet ist, und das Fenster enthält nun Informationen über den Zielcomputer.

### 3.1.9
* Es wurde ein Problem behoben, das manchmal dazu führte, dass eine RIM-Sitzung beim Versuch, die Verbindung wiederherzustellen, hängen blieb.

### 3.1.8
* Es wurde ein Problem behoben, das manchmal den Start des Remotezugriffsmoduls verhinderte.

### 3.1.7
* Bei der Auswahl eines Zielcomputers für eine unbeaufsichtigte oder angeforderte Sitzung können Sie nun Ihre Zielcomputer durchsuchen. Drücken Sie dazu wie bisher auf dem Bildschirm „Hilfe bereitstellen“ auf „Computer auswählen“, wechseln Sie zum Feld „Suchen“, geben Sie den Suchbegriff ein und drücken Sie entweder die Eingabetaste oder die Schaltfläche „Suchen“.

### 3.1.6
* Es wurde ein Problem behoben, das manchmal dazu führte, dass die JAWS-Sprachausgabe auf dem sicheren Desktop nicht unterbrochen werden konnte.

### 3.1.5
* Es wurde ein Fehler in der visuellen Darstellung behoben, der dazu führte, dass sich die Liste der unbeaufsichtigten Zielcomputer mit den darunter liegenden Schaltflächen überlappte.

### 3.1.4
* Unternehmenskunden können RIM jetzt mit einem MSI-basierten Installationsprogramm bereitstellen.

### 3.1.3
* Es wurde ein Fehler behoben, der zum Absturz von RIM führen konnte, wenn das RIM-Menü geöffnet war, während eine Sitzung erneut verbunden wurde.

### 3.1.2
* Der visuelle Stil wurde angepasst, um für sehbehinderte Benutzer besser zugänglich zu sein.
* Es wurden Fehlermeldungen hinzugefügt, wenn Pflichtfelder leer gelassen werden.

### 3.1.1
* Wenn Sie im Fenster „Hilfe bereitstellen“ auf die Schaltfläche „RIM-Dashboard“ klicken, bleibt dieses Fenster nun geöffnet.

### 3.1.0
Diese Version enthält ein vollständiges Verwaltungs-Dashboard für Controller.
* Alle Abonnenten haben Zugriff auf das Dashboard, wobei die Funktionen je nach Abonnementstufe variieren.
* Die In-App-Funktionen zum Umbenennen, Löschen und Erstellen von Installationsprogrammen für unbeaufsichtigte Maschinen wurden entfernt, da dies nun Teil des Dashboards ist.
* Die Willkommensseite wurde wieder eingeführt.

### 3.0.8
Es wurde ein Fehler behoben, der manchmal dazu führte, dass die Remote-Bildschirmausgabe nicht funktionierte, wenn eine Benutzerkontensteuerungsaufforderung angezeigt wurde.
### 3.0.7
* Dies ist ein Update zur Fehlerbehebung.

### 3.0.6
* Benutzer mit einem RIM Enterprise-Abonnement oder einem RIM Pro-Abonnement mit dem Enterprise-Add-on können nun vollständig im Hintergrund laufende, vorkonfigurierte Installationsprogramme für den unbeaufsichtigten Zugriff erstellen. Um ein Installationsprogramm im Hintergrundmodus auszuführen, unabhängig davon, ob es sich um das Standardinstallationsprogramm oder ein vorkonfiguriertes Installationsprogramm aus einem Unternehmenskonto handelt, verwenden Sie beim Ausführen des Installationsprogramms den Befehlszeilenschalter /S (beachten Sie die Großschreibung).

### 3.0.5
* Dies ist ein Bugfix-Update.

### 3.0.4
* Dieses Update stellt die Kompatibilität mit Windows 7 wieder her.

### 3.0.3
* RIM zeigt nicht mehr fälschlicherweise die Schaltfläche „Installationsprogramm für unbeaufsichtigten Zugriff erstellen” an, wenn Sie kein Abonnement haben.

### 3.0.2
* Wenn Sie einen oder mehrere RIM-Pässe erworben haben, können Sie nun steuern, ob für eine bestimmte Sitzung ein RIM-Pass oder die kostenlose Stufe verwendet wird und welcher Pass verwendet wird. RIM teilt Ihnen auch mit, wie viel Zeit auf Ihrem ausgewählten Pass noch verbleibt.

### 3.0.1
* Dieses Update bietet Unterstützung für die neuen persönlichen RIM-Pässe, mit denen Sie RIM für bis zu 24 Stunden für nur 10 US-Dollar für technischen Remote-Support oder Schulungen nutzen können. [Probieren Sie noch heute einen RIM-Pass aus.](https://getrim.app/signup/pass)
* Die Schaltfläche, die früher „Vorkonfigurierten Zielinstallationsprogramm erstellen” hieß, heißt jetzt „Installationsprogramm für unbeaufsichtigten Zugriff erstellen”.
* Beim Umbenennen eines unbeaufsichtigten Zielcomputers wird nun der vorhandene Name ausgefüllt und zur einfachen Bearbeitung ausgewählt.

### 3.0.0
Mit dieser Version endet die öffentliche Beta-Phase von RIM. Die RIM-Preispläne und Einschränkungen für die kostenlose Nutzung sind nun in Kraft. Für kostenlose Konten zeigt RIM nun einen Nutzungszähler zusammen mit einer Schaltfläche zum Kauf an.
### 0.12.2
Es wurde ein Fehler behoben, der dazu führte, dass die neue Tastenkombination Windows+Umschalt+Rücktaste in einigen Situationen nicht funktionierte.
### 0.12.1
* Der Hotkey Strg+Umschalt+Rücktaste ist in RIM nicht mehr aktiv. Der offizielle Hotkey lautet nun Windows+Umschalt+Rücktaste. Windows+Umschalt+Escape bleibt während einer Sitzung ebenfalls verfügbar.
* Der neue Haupt-Hotkey von RIM, Windows+Umschalt+Rücktaste, ist nun auf allen Rechnern, auf denen RIM installiert ist, jederzeit aktiv, es sei denn, eine andere Anwendung beansprucht den Hotkey zuerst. Wenn eine RIM-Sitzung auf dem Controller aktiv ist, öffnet dieser Hotkey das RIM-Menü, wenn das Sitzungsfenster im Vordergrund ist, oder fokussiert das Sitzungsfenster, wenn es im Hintergrund ist. Auf dem Zielgerät öffnet dieser Hotkey das RIM-Zielmenü, während die Sitzung aktiv ist. Auf beiden Seiten öffnet dieser Hotkey, wenn keine Sitzung aktiv ist, das RIM-Fenster, um eine neue Sitzung zu starten.
* Bei der Anforderung eines unbeaufsichtigten Zugriffs fokussiert RIM nun zuverlässig den Genehmigungsdialog auf dem Zielcomputer.
* Das Modul für den Fernzugriff wurde aktualisiert.

### 0.12.0
Sie können nun ein vorkonfiguriertes Installationsprogramm erstellen, das alle Computer, auf denen es installiert ist, automatisch als unbeaufsichtigte Ziele in Ihrem RIM-Konto registriert.

Verwenden Sie dazu die neue Schaltfläche „Vorkonfiguriertes Zielinstallationsprogramm erstellen“ auf dem Bildschirm „Hilfe bereitstellen“. Anschließend müssen Sie einen Basisnamen für die Zielcomputer eingeben. RIM fügt diesem Basisnamen automatisch den Hostnamen jedes Zielcomputers hinzu. Sobald die Zielcomputer registriert sind, können Sie sie wie jedes unbeaufsichtigte Ziel umbenennen. Sie können auch das Ablaufdatum des Installationsprogramms festlegen, das zwischen 7 und 30 Tagen liegen muss. Sobald RIM das Installationsprogramm erstellt hat, erhalten Sie einen Link, den Sie entweder direkt an die Zielbenutzer weitergeben oder selbst herunterladen können, um das Installationsprogramm auf andere Weise weiterzugeben.

Beachten Sie, dass den Zielbenutzern beim Ausführen des vorkonfigurierten Installationsprogramms zunächst ein Bestätigungsdialogfeld angezeigt wird, in dem erklärt wird, was das Installationsprogramm tut. Dieses Dialogfeld enthält den Namen des Controllers, der das Installationsprogramm erstellt hat. Dieser Bestätigungsschritt soll Missbrauch verhindern. Für Unternehmenskunden wird ein vollständig stiller Modus verfügbar sein.

### 0.11.6
* RIM bietet jetzt eine neue Option, mit der Sie einen Computer für den unbeaufsichtigten Zugriff unter Ihrem RIM-Konto registrieren können, ohne eine Remote-Sitzung mit einem Schlüsselwort starten zu müssen. Klicken Sie auf dem Computer, den Sie registrieren möchten, auf der Seite „Hilfe erhalten“ auf die neue Schaltfläche „Diesen Computer für unbeaufsichtigten Zugriff registrieren“. Melden Sie sich dann bei Ihrem RIM-Konto an, falls Sie dies auf diesem Computer noch nicht getan haben, geben Sie einen Namen für den Computer ein, und schon sind Sie fertig. Beachten Sie, dass wir noch daran arbeiten, das RIM-Installationsprogramm so anzupassen, dass eine Massenbereitstellung möglich ist und die Einrichtung des unbeaufsichtigten Zugriffs auf Computer, auf die Sie noch keinen Zugriff haben, so einfach wie möglich wird.
* RIM verhindert nun, dass Sie versuchen, eine unbeaufsichtigte Verbindung zu demselben Computer herzustellen, auf dem Sie den Controller ausführen.
* Wir experimentieren derzeit mit einem dritten Hotkey zum Öffnen des RIM-Menüs: Windows+Umschalt+Rücktaste. Das bedeutet, dass es nun drei Optionen gibt: Windows+Umschalt+Esc, Windows+Umschalt+Rücktaste und Strg+Umschalt+Rücktaste. Wir haben noch keine endgültige Entscheidung getroffen, welche Hotkeys beibehalten werden sollen, werden dies aber vor Ende der öffentlichen Beta-Phase tun. Wir freuen uns über Ihr Feedback.
* Die meisten Optionen im RIM-Menü verfügen nun über Ein-Buchstaben-Tastenkombinationen, die Sie nach dem Öffnen des Menüs drücken können.
* Dieses Update bietet eine zuverlässigere Lösung für die sporadisch auftretenden Probleme, die Benutzer gemeldet haben, wenn JAWS die Tastatureingaben von RIM nicht erkennt. Bitte teilen Sie uns mit, wenn Sie weiterhin Probleme mit diesem Szenario haben.

### 0.11.5
* Dieses Update führt zwei neue Hotkeys ein. Windows+Umschalt+D trennt die Sitzung, wenn es auf einer der beiden Seiten gedrückt wird. Für den Controller dreht Windows+Umschalt+F die Sitzung um.
* Dieses Update enthält Schutzmaßnahmen gegen das Hängenbleiben von Tastaturmodifikatoren auf beiden Rechnern.
* Es gibt eine neue Menüoption „Zielrechner sperren”, die dem Drücken von Windows+L auf dem Zielrechner entspricht.
* RIM verfügt nun über ein „Info”-Fenster, in dem Sie die Versionsnummer überprüfen, Pneuma Solutions für Support und Feedback kontaktieren, das Handbuch lesen und das Versionsänderungsprotokoll anzeigen können.
* RIM fordert am Ende einer Sitzung oder beim Schließen des Fensters „Hilfe bereitstellen“ nicht mehr zur Abgabe von Feedback auf.

### 0.11.4
Dieses Update behebt einige Fehler beim Neustart- und Wiederverbindungsprozess. Wir haben noch nicht alle gemeldeten Probleme diagnostiziert, konnten jedoch bei unseren eigenen Tests einige Probleme finden und beheben.
### 0.11.3
* RIM aktualisiert sich nun auf allen Rechnern im Hintergrund. RIM zeigt nun die aktuell installierte Versionsnummer in der QuickInfo seines Taskleistensymbols an. Dies ist das letzte Update, das Benutzer unabhängig von der Rechnerkonfiguration manuell installieren müssen.
* Wenn der Zielcomputer neu gestartet wird, aber die Funktion „Neustart und erneute Verbindung” von RIM nicht verwendet wird, fragt RIM nun den Controller, ob er nach dem Neustart eine erneute Verbindung zum Ziel herstellen möchte. Wenn der Controller mit „Nein” antwortet, wartet das Ziel nach dem Neustart nicht auf eine erneute Verbindung.

### 0.11.2
* RIM aktualisiert sich nun im Hintergrund auf unbeaufsichtigten Zielcomputern. Dies ist das letzte Update, das Sie manuell auf Ihren unbeaufsichtigten Zielen installieren müssen.
* Bei Updates vor einer interaktiven Sitzung muss RIM nicht mehr die Benutzerkontensteuerung durchlaufen. Das bedeutet, dass Nicht-Administratoren RIM aktualisieren können, sobald es von einem Administrator für sie installiert wurde.

### 0.11.1
Dies ist ein Update zur Fehlerbehebung. Bitte beachten Sie, dass unbeaufsichtigte Ziele weiterhin manuell aktualisiert werden müssen, indem Sie das neueste RIM-Installationsprogramm ausführen. Nach Abschluss der Installation können Sie nun jedoch eine Verbindung zu einem unbeaufsichtigten Ziel herstellen, ohne dass jemand das Fenster „Hilfe erhalten“ schließen muss.
### 0.11.0
* Die Funktion „Neustart und erneute Verbindung” wurde implementiert. Mit der neuen Option „Neustart und erneute Verbindung” im RIM-Menü des Controllers können Sie einen Standard- oder Not-Neustart durchführen und die Sitzung nach Abschluss des Neustarts automatisch fortsetzen. RIM stellt auch dann eine erneute Verbindung her, wenn der Zielcomputer aus einem anderen Grund neu gestartet wird, z. B. wenn ein Installationsprogramm den Computer neu startet.
* RIM versucht auch automatisch, eine erneute Verbindung herzustellen, wenn die Verbindung aus irgendeinem Grund unterbrochen wird. Dies ist hilfreich bei unzuverlässigen Internetverbindungen.
* Wenn Sie eine unbeaufsichtigte Sitzung ausführen oder „Neustart und erneute Verbindung“ verwenden, können Sie RIM jetzt auch verwenden, während sich der Zielcomputer auf dem Windows-Anmeldebildschirm befindet. Beachten Sie, dass RIM die Sitzung erneut verbinden muss, wenn der Zielcomputer vom Anmeldebildschirm zum Hauptdesktop wechselt. Dies ist eine bekannte Einschränkung der aktuellen Implementierung, die wir möglicherweise in einem zukünftigen Update beheben werden.
* RIM ist jetzt voll funktionsfähig, wenn der Zielbenutzer keine Administratorrechte auf dem Zielcomputer hat, solange der Remote Incident Manager Host-Dienst auf diesem Computer ausgeführt wird. Beachten Sie, dass für die Installation von RIM-Updates weiterhin Administratorrechte erforderlich sind. Beachten Sie auch, dass neue Versionen von RIM weiterhin manuell auf unbeaufsichtigten Zielen installiert werden müssen. Wir arbeiten an Lösungen für beide Probleme.

### 0.10.10.0
Wenn Sie den unbeaufsichtigten Zugriff auf einen oder mehrere Zielcomputer eingerichtet haben, können Sie jetzt eine Verknüpfung erstellen, um direkt auf einen dieser Computer zuzugreifen. Klicken Sie einfach auf die Schaltfläche „Verknüpfung erstellen” unter der Liste der unbeaufsichtigten Ziele, und RIM erstellt eine Verknüpfung auf Ihrem Desktop.
### 0.10.9
RIM funktioniert jetzt auch auf Zielcomputern ohne Audiogerät. In Kombination mit dem bestehenden Fernzugriffsmodul eröffnet dies den Zugriff auf Server und virtuelle Maschinen, auf die zuvor mit einem Screenreader nur über RDP und dessen sehr suboptimale Audioausgabe zugegriffen werden konnte.
### 0.10.8
* Wenn Sie das erste Jahr Ihres RIM Pro-Abonnements noch nicht vorbestellt haben, können Sie dies jetzt ganz einfach über die neue Schaltfläche auf dem Bildschirm „Hilfe anbieten“ tun. Wir führen auch neue Preisoptionen für das Hinzufügen zusätzlicher Controller und gleichzeitiger Kanäle zu einem RIM Pro-Abonnement ein. Sie können bis zu zwei zusätzliche Controller-Benutzer und zwei zusätzliche Kanäle für jeweils 500 US-Dollar pro Jahr hinzufügen. RIM ist während der öffentlichen Beta-Phase weiterhin kostenlos, aber wenn Sie Ihr Abonnement jetzt vorbestellen, können Sie den Dienst nach Ablauf der öffentlichen Beta-Phase am 1. September ohne Unterbrechung weiter nutzen.
* Wir führen Control+Shift+Backspace als alternativen Hotkey zum Öffnen des RIM-Menüs auf beiden Seiten der Verbindung ein. Wir unterstützen weiterhin Windows+Shift+Escape, aber wir planen, Control+Shift+Backspace als einzigen Hotkey für alle Phasen von RIM zu standardisieren.
* Wenn Sie auf dem Bildschirm „Hilfe erhalten“ auf die Schaltfläche „Stattdessen Hilfe bereitstellen“ klicken, legt RIM diese Option nicht mehr als Standard fest, bis Sie sich bei Ihrem RIM-Konto angemeldet haben.
* Einige JavaScript-Fehler, die in seltenen Fällen auftraten, wurden behoben.

### 0.10.7
Sie können nun das Mausrad in einer Remote-Sitzung verwenden.
### 0.10.6
Hoffentlich wurden alle verbleibenden Probleme behoben, die zum Absturz des Installationsprozesses führten.
### 0.10.5
Diese Version enthält einige Änderungen unter der Haube. Insbesondere haben wir Aktualisierungen der Installation und der zugrunde liegenden Pakete vorgenommen, um Probleme zu beheben, die zum Absturz der Installation führten.
### 0.10.4
* Im RIM-Menü gibt es jetzt eine Option, um Control+Alt+Delete an den Zielcomputer zu senden.
* Sie können das RIM-Menü jetzt durch einen Rechtsklick auf die Titelleiste des Sitzungsfensters öffnen. Wir planen, die Option hinzuzufügen, das RIM-Menü auch durch Klicken auf das RIM-Symbol am linken Ende der Titelleiste zu öffnen.

### 0.10.3
* Der Controller kann jetzt eine unbeaufsichtigte Sitzung umschalten.
* Der Controller kann jetzt einen unbeaufsichtigten Zielcomputer umbenennen.
* Es wurde ein Fehler behoben, der manchmal dazu führte, dass die Sitzung nach dem Kopieren in die Zwischenablage nicht mehr reagierte.
* Es wurde ein Fehler behoben, der manchmal dazu führte, dass das Starten des Remote-Zugriffsmoduls mit einem JavaScript-Fehler fehlschlug.

### 0.10.2
Es wurde ein Konflikt zwischen dem RIM Client Support NVDA-Add-on und dem Dropbox-Add-on behoben.
### 0.10.1
* Sie können jetzt das erste Jahr Ihres Abonnements für RIM Pro für 999 $ vorbestellen. Der Link zur Vorbestellung wird im Feedback-Fenster angezeigt, wenn Sie eine Remote-Sitzung beenden. Oder Sie können dem direkten Link hier folgen: [RIM Pro vorbestellen.](https://getrim.app/signup/rim_preorder)
* Wenn Sie unbeaufsichtigte Zielcomputer in Ihrem RIM-Konto haben, wird die Schaltfläche zum Starten einer unbeaufsichtigten Sitzung nun zuverlässiger auf dem Bildschirm „Hilfe bereitstellen“ angezeigt.
* Beim Umschalten einer Sitzung kann der ursprüngliche Controller die Sitzung nun zurückumschalten, indem er das RIM-Menü mit Windows+Umschalt+Escape öffnet.

